package io.vopenia.sdk.utils

import com.vopenia.sdk.utils.Dispatchers as DPC

object Dispatchers {
    val IO = DPC.IO
    val Main = DPC.Main
    val Default = DPC.Default
    val Unconfined = DPC.Unconfined
}
